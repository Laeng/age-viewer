services:
  age-viewer:
    build:
      context: .
      args:
        NODE_VERSION: 20
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped